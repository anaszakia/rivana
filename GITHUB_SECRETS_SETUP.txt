╔════════════════════════════════════════════════════════════════╗
║  🚀 SETUP GITHUB SECRETS - LANGKAH TERAKHIR AUTO DEPLOY        ║
╚════════════════════════════════════════════════════════════════╝

✅ File deploy.yml sudah ter-push ke GitHub
✅ SSH key sudah di-generate di server  
✅ SSH connection test: SUCCESS

❌ Yang BELUM: GitHub Secrets belum disetup!

════════════════════════════════════════════════════════════════

📝 LANGKAH SETUP GITHUB SECRETS (5 MENIT):

1️⃣  BUKA BROWSER dan ke URL ini:
   
   https://github.com/anaszakia/rivana/settings/secrets/actions
   
   (Ganti 'anaszakia/rivana' dengan username/repo Anda yang benar)

─────────────────────────────────────────────────────────────────

2️⃣  KLIK tombol "New repository secret" (hijau)

─────────────────────────────────────────────────────────────────

3️⃣  TAMBAHKAN SECRET PERTAMA:
   
   Name: HOST
   
   Value: 31.97.186.233
   
   Klik "Add secret"

─────────────────────────────────────────────────────────────────

4️⃣  KLIK LAGI "New repository secret"
   
   Name: USER
   
   Value: root
   
   Klik "Add secret"

─────────────────────────────────────────────────────────────────

5️⃣  KLIK LAGI "New repository secret" (YANG PALING PENTING!)
   
   Name: KEY
   
   Value: (SSH ke server dan copy private key)
   
   ⚠️  CARA COPY PRIVATE KEY:
   
   # SSH ke server:
   ssh root@31.97.186.233
   
   # Tampilkan private key:
   cat ~/.ssh/github_deploy
   
   # Copy SELURUH output (termasuk BEGIN dan END)
   # Paste ke GitHub Secret KEY
   
   Contoh format yang benar:
   -----BEGIN OPENSSH PRIVATE KEY-----
   b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAACFwAAAAdzc2gtcn
   NhAAAAAwEAAQAAAgEAthB4ZVJqYWE5cGVxbnNmZWVxd2Vmd2Vmd2Vmd2Vmd2Vmd2Vmd2Vm
   ... (banyak baris)
   ... (copy semua sampai baris terakhir)
   -----END OPENSSH PRIVATE KEY-----
   
   Klik "Add secret"

════════════════════════════════════════════════════════════════

6️⃣  VERIFIKASI SECRETS SUDAH ADA

   Setelah menambahkan 3 secrets, Anda akan melihat:
   
   ✅ HOST
   ✅ USER  
   ✅ KEY
   
   Di halaman: 
   https://github.com/anaszakia/rivana/settings/secrets/actions

════════════════════════════════════════════════════════════════

7️⃣  ENABLE GITHUB ACTIONS (jika belum)

   a. Buka: 
      https://github.com/anaszakia/rivana/settings/actions
   
   b. Pilih:
      ⚪ "Allow all actions and reusable workflows"
   
   c. Klik "Save"

════════════════════════════════════════════════════════════════

8️⃣  TEST AUTO DEPLOY!

   # Di komputer lokal (Windows PowerShell):
   cd e:\laragon\www\it_river_dna
   
   # Buat perubahan kecil:
   echo "# Test auto deploy $(Get-Date)" >> README.md
   
   # Commit dan push:
   git add .
   git commit -m "Test auto deploy"
   git push origin main

════════════════════════════════════════════════════════════════

9️⃣  MONITOR DEPLOY DI GITHUB ACTIONS:

   a. Buka:
      https://github.com/anaszakia/rivana/actions
   
   b. Lihat workflow "Deploy to Server"
   
   c. Status:
      🟡 Yellow = Sedang berjalan (tunggu 2-5 menit)
      🟢 Green  = SUKSES! ✅
      🔴 Red    = Gagal (klik untuk lihat error)
   
   d. Klik workflow untuk lihat detail log real-time

════════════════════════════════════════════════════════════════

🎯 CARA KERJA SETELAH SETUP:

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Anda edit code di lokal                                │
│  2. git push origin main                                   │
│  3. GitHub Actions otomatis triggered                      │
│  4. Build NPM assets                                       │
│  5. Upload ke server via SCP                               │
│  6. Run composer install                                   │
│  7. Run migrations                                         │
│  8. Clear cache                                            │
│  9. Set permissions                                        │
│  10. ✅ LIVE di production!                                │
│                                                             │
│  TIDAK PERLU SSH KE SERVER LAGI!                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING:

❌ "Workflow tidak muncul di Actions"
   → Pastikan GitHub Actions sudah enabled
   → Refresh halaman GitHub
   → Cek file .github/workflows/deploy.yml ada di repo

❌ "Permission denied (publickey)"
   → Copy ulang private key dari server
   → Pastikan copy LENGKAP (BEGIN sampai END)
   → Paste ulang ke GitHub Secret KEY

❌ "Host key verification failed"
   → SSH ke server dan jalankan:
     echo "Host *
       StrictHostKeyChecking no
       UserKnownHostsFile=/dev/null" >> ~/.ssh/config
     chmod 600 ~/.ssh/config

❌ "npm ci failed"
   → Normal, akan fallback ke npm install
   → Deploy tetap lanjut

════════════════════════════════════════════════════════════════

📋 CHECKLIST FINAL:

□ GitHub Secrets HOST sudah ditambahkan
□ GitHub Secrets USER sudah ditambahkan  
□ GitHub Secrets KEY sudah ditambahkan (private key lengkap)
□ GitHub Actions sudah enabled
□ Test push sudah dilakukan
□ Monitor di Actions → workflow "Deploy to Server" muncul
□ Status deploy: 🟢 GREEN (SUKSES!)

════════════════════════════════════════════════════════════════

✅ SETELAH SETUP BERHASIL:

Setiap kali Anda push ke main branch:

  git add .
  git commit -m "Update feature"
  git push origin main

Deploy akan berjalan OTOMATIS! 🎉

Tidak perlu SSH ke server lagi!
Tidak perlu git pull manual!
Tidak perlu composer install manual!

SEMUA OTOMATIS! ✨

════════════════════════════════════════════════════════════════

📞 BUTUH BANTUAN?

1. Cek GitHub Actions logs untuk lihat error detail
2. Cek Laravel logs di server: 
   tail -f /usr/share/nginx/rivana.cloud/public_html/rivana/storage/logs/laravel.log
3. Cek Nginx logs:
   tail -f /var/log/nginx/error.log

════════════════════════════════════════════════════════════════

Status: ⏳ Menunggu GitHub Secrets setup
Next:   Setup 3 GitHub Secrets di browser
ETA:    5 menit

════════════════════════════════════════════════════════════════
