╔════════════════════════════════════════════════════════════════╗
║  🔍 TROUBLESHOOTING: GitHub Actions Tidak Muncul              ║
╚════════════════════════════════════════════════════════════════╝

📊 SITUASI SAAT INI:
• Push berhasil ke GitHub ✅
• GitHub Secrets sudah disetup ✅
• Commit di lokal: 90f6add (Test auto deploy from GitHub Actions)
• Commit di server: a36adcf, e2a419b (commit lama)
• GitHub Actions workflow: TIDAK MUNCUL ❌

════════════════════════════════════════════════════════════════

🐛 KEMUNGKINAN PENYEBAB & SOLUSI:

─────────────────────────────────────────────────────────────────
1️⃣  GITHUB ACTIONS BELUM ENABLED
─────────────────────────────────────────────────────────────────

✅ SOLUSI:

a. Buka browser → Repository Settings:
   https://github.com/anaszakia/rivana/settings/actions

b. Di section "Actions permissions", pilih:
   ⚪ "Allow all actions and reusable workflows"
   
c. Klik "Save"

d. Refresh halaman Actions:
   https://github.com/anaszakia/rivana/actions

─────────────────────────────────────────────────────────────────
2️⃣  WORKFLOW FILE TIDAK TER-PUSH KE .github/workflows/
─────────────────────────────────────────────────────────────────

✅ CEK & SOLUSI:

a. Buka di GitHub:
   https://github.com/anaszakia/rivana/tree/main/.github/workflows

b. Pastikan ada file: deploy.yml

c. Jika TIDAK ADA, push ulang:
   
   cd e:\laragon\www\it_river_dna
   git add .github/workflows/deploy.yml
   git commit -m "Add deploy workflow"
   git push origin main

─────────────────────────────────────────────────────────────────
3️⃣  SYNTAX ERROR DI deploy.yml
─────────────────────────────────────────────────────────────────

✅ CEK & SOLUSI:

a. Validasi YAML syntax online:
   https://www.yamllint.com/

b. Copy isi deploy.yml dan paste di yamllint

c. Jika ada error, perbaiki dan push ulang

─────────────────────────────────────────────────────────────────
4️⃣  BRANCH SALAH (Bukan main)
─────────────────────────────────────────────────────────────────

✅ CEK:

git branch --show-current

Harus menunjukkan: main

Jika bukan main (misal: master), edit deploy.yml:

on:
  push:
    branches:
      - master  # ← Ganti sesuai branch Anda

─────────────────────────────────────────────────────────────────
5️⃣  GITHUB ACTIONS MASIH PROCESSING (Delayed)
─────────────────────────────────────────────────────────────────

✅ TUNGGU & CEK:

Kadang GitHub Actions delayed 1-2 menit. Tunggu sebentar lalu:

a. Refresh halaman:
   https://github.com/anaszakia/rivana/actions

b. Tekan Ctrl+Shift+R (hard refresh)

c. Cek tab "All workflows"

════════════════════════════════════════════════════════════════

🔧 QUICK FIX - MANUAL TRIGGER WORKFLOW:

Jika workflow tidak muncul otomatis, trigger manual:

1. Edit file deploy.yml, tambahkan workflow_dispatch:

   on:
     push:
       branches:
         - main
     workflow_dispatch:  # ← TAMBAHKAN INI

2. Commit dan push:
   git add .github/workflows/deploy.yml
   git commit -m "Add manual workflow trigger"
   git push origin main

3. Buka GitHub Actions:
   https://github.com/anaszakia/rivana/actions

4. Klik workflow "Deploy to Server"

5. Klik tombol "Run workflow" (hijau)

6. Pilih branch: main

7. Klik "Run workflow"

════════════════════════════════════════════════════════════════

🚀 FORCE TRIGGER WORKFLOW (Test Push):

Jika masih tidak muncul, coba force trigger dengan push baru:

cd e:\laragon\www\it_river_dna

# Buat perubahan dummy
echo "# Force trigger - $(date)" >> README.md

# Commit dan push
git add README.md
git commit -m "Force trigger GitHub Actions"
git push origin main

# Tunggu 30 detik, lalu cek:
# https://github.com/anaszakia/rivana/actions

════════════════════════════════════════════════════════════════

📋 CHECKLIST DEBUG:

Cek satu per satu:

□ GitHub Actions enabled?
  → https://github.com/anaszakia/rivana/settings/actions
  
□ File deploy.yml ada di GitHub?
  → https://github.com/anaszakia/rivana/tree/main/.github/workflows
  
□ Branch name benar (main)?
  → git branch --show-current
  
□ Sudah tunggu 2 menit?
  → Refresh halaman Actions
  
□ Syntax YAML valid?
  → Paste ke yamllint.com
  
□ Secrets semua ada (HOST, USER, KEY)?
  → https://github.com/anaszakia/rivana/settings/secrets/actions

════════════════════════════════════════════════════════════════

🎯 SOLUSI TERCEPAT - JALANKAN SEKARANG:

1. Pastikan Actions enabled:
   https://github.com/anaszakia/rivana/settings/actions
   → Allow all actions

2. Edit deploy.yml (tambah workflow_dispatch)

3. Push ulang

4. Manual trigger di GitHub Actions

════════════════════════════════════════════════════════════════

📞 JIKA MASIH TIDAK MUNCUL:

1. Screenshot halaman GitHub Actions:
   https://github.com/anaszakia/rivana/actions

2. Screenshot halaman Settings → Actions:
   https://github.com/anaszakia/rivana/settings/actions

3. Jalankan command ini dan kirim output:
   git log --oneline -5
   git branch --show-current
   git remote -v

════════════════════════════════════════════════════════════════

Status: ⚠️  Workflow belum muncul
Action: Enable GitHub Actions + Manual trigger
ETA: 2-5 menit

════════════════════════════════════════════════════════════════
